- name: Build HTML from Marp
        run: |
          # Marpで一括変換
          npx @marp-team/marp-cli@latest --input-dir . --output build --html

          # GitHub PagesのJekyll処理を無効化（これがないとHTMLが無視されて404になります）
          touch build/.nojekyll

          # デバッグ用：生成されたフォルダ構造をログに出力
          ls -R build
